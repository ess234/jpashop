#spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        show_sql: true #(system.out으로 hibernate 로그)
#        format_sql: true
logging:
  level:
    org.hibernate.SQL: debug #logger로 hibernate 로그
    #  org.hibernate.type: trace